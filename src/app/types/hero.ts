export interface HeroSectionModel {
  heroImageUrl: string;
  name: string;
  heading: string;
  typingSegments: Array<string | number>;
  description: string;
  buttonLabel: string;
  buttonUrl: string;
}
